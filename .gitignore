# Bulk data files (download from id.loc.gov)
data/hubs.bibframe.ttl
data/hubs.bibframe.ttl.gz
data/hubs.bibframe-*.ttl
data/hubs.bibframe-*.ttl.gz

# Docker runtime
docker/local/cache/

# Production secrets (never commit; see .env.example)
.env

# IDE / workspace
*.code-workspace
.DS_Store

# Composer
vendor/

